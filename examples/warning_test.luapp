-- Test file for warnings

-- Unused variable warning
local unused_var = 42

-- This should work fine
local used_var = 10
print(tostring(used_var))

-- Variables starting with _ are ignored
local _ignored = 100

print("Done!")
